<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/col_tool</item>
        <item name="colorPrimaryDark">@color/col_tool</item>
        <item name="colorAccent">@color/col_sec</item>

        <item name="actionModeBackground">@color/text_on_white</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

</resources>
